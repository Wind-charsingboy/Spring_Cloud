spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Wind-charsingboy/config-repo.git
          userName: 1322312879@qq.com
          password: ab618622
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#/bus-refresh interface expose out
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh, bus-env




