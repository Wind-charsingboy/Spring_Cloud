spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
#sign up to eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  #zuul:
  #  routes:
  #    myProduct:
  #      path: /myProduct/**
  #      serviceId: product
zuul:
  routes:
    product: /myProduct/**
  ignored-patterns:
    - **/product/decreaseStock







